{
  "name": "auu-mcp-rg",
  "version": "1.2.3",
  "description": "A Windows-compatible MCP server for ripgrep with root directory restriction support",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "auu-mcp-rg": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "setup": "npm install && npm run build",
    "test": "node test-server.js",
    "dev": "npm run build && npm start",
    "postbuild": "node -e \"const fs = require('fs'); const content = fs.readFileSync('dist/index.js', 'utf8'); fs.writeFileSync('dist/index.js', '#!/usr/bin/env node\\n' + content);\""
  },
  "keywords": [
    "mcp",
    "ripgrep",
    "rg",
    "search",
    "windows",
    "security",
    "sandbox",
    "root-restriction"
  ],
  "author": "Auu",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
